#include "main.h"
/**
 * _abs(int) -  computes the absolute value of an integer.
 *
 * return: abs_val
 */
int _abs(int)
{
	int c;

	if (c < 0)
	{
		int abs_val;

		abs_val = c * -1;
		return (abs_val);
	}
}
