#include <main.h>
/**
 * Write a function that computes the absolute value of an integer.
 * Prototype: int _abs(int);
 */
int _abs(int)
{
	int c;

	if(c < 0)
	{
		int abs_val;
		abs_val = c * -1;
		return (abs_val);
	}
}
